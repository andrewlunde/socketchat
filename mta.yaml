## Generated mta.yaml based on yo sap-partner-eng version 0.3.11
## appName = chat
## language=nodejs; multiTenant=false
## approuter=chat-app
_schema-version: '3.1'
ID: chat
version: 0.0.1
description: Simple Chat over Websockets
parameters:
   enable-parallel-deployments: true
  
build-parameters:
  before-all:
#   - builder: custom
#     commands:
#      - npm install

modules:

### New Modules Here ###

# cf push chat-srv -p srv -n chat-srv -d cfapps.us10.hana.ondemand.com -k 128M -m 128M -b nodejs_buildpack -u none
 - name: chat-srv
   type: nodejs
   path: srv
   build-parameters:
      ignore: ["node_modules/"]
   parameters:
      buildpack: nodejs_buildpack
      memory: 256M
      disk-quota: 512M
      #host: ${org}-${space}-chat-srv
      #domain: cfapps.us10.hana.ondemand.com
   provides:
    - name: chat_api
      properties:
         url: ${default-url}
   requires:
    - name: chat-uaa

# cf push chat-app -p app -n chat-app -d cfapps.us10.hana.ondemand.com -k 128M -m 128M -b nodejs_buildpack 
 - name: chat-app
   type: html5
   path: app
   build-parameters:
      # Don't package the dependencies if you're running into EINTEGRITY issues
      ignore: ["default-env.json","package-lock.json", "node_modules/"]
      # Suppress the calling of npm install by listing no commands
      builder: custom
      commands: []
   parameters:
      buildpack: nodejs_buildpack
      #host: ${org}-${space}-chat-app
      #domain: cfapps.us10.hana.ondemand.com
      #routes:
      #  - route: chat-app-${space}-${app-name}.${default-domain}
      disk-quota: 256M
      memory: 256M
   provides:
    - name: Router_api
      properties:
         url: ${default-url}
         application: ${app-name}
# CDS-MTX
   requires:
   # cf bind-service chat-app CHAT_UAA
    - name: chat-uaa
   ### New Destinations Here ###
    - name: chat_api
      group: destinations
      properties:
         name: chat_be
         url: ~{url}
         forwardAuthToken: true

resources:

# cf create-service xsuaa application CHAT_UAA -c ./cds-security.json
 - name: chat-uaa
   type: org.cloudfoundry.managed-service
   requires:
    - name: Router_api
   properties:
      XSAPPNAME: ${xsuaa-app}
   parameters:
      # Re-run the following command after changing any @(requires: []) definitions in srv/*.cds
      # Run this from the root of the project and uncomment the path:
      # cds compile srv/ --to xsuaa,json > cds-security.json
      #path: ./cds-security.json
      # The combination of path: and config.scopes: Doesn't seem work.  Use one or the other.
      # cds compile srv/ --to xsuaa,yaml and make sure the config: below matches
      #path: ./cds-security.json
      #path: ./xs-security.json
      service: xsuaa
      service-plan: application
      service-name: CHAT_UAA
      xsuaa-app: ${space}-~{Router_api/application}
      # Define within config what used to be in the xs-security.json file.  path: now pulls in cds scopes/role templates
      # Convert with https://www.json2yaml.com/
      # https://github.wdf.sap.corp/pages/cap/guides/authorization#role-assignments-with-xsuaa
      config:
         xsappname: ${xsuaa-app}
         ### tenant-mode
         tenant-mode: dedicated
         description: Security profile of the chat application
         scopes:
          - name: "$XSAPPNAME.Callback"
            description: With this scope set, the callbacks for tenant onboarding, offboarding and getDependencies can be called.
            grant-as-authority-to-apps: 
             - "$XSAPPNAME(application,sap-provisioning,tenant-onboarding)"
          - name: "$XSAPPNAME.Administrator"
            description: Administrate the application
          - name: "$XSAPPNAME.User"
            description: Use the application
         role-templates:
          - name: Administrator
            description: Administrator
            scope-references:
             - "$XSAPPNAME.Administrator"
          - name: User
            description: User
            scope-references:
             - "$XSAPPNAME.User"
         oauth2-configuration:
            redirect-uris: 
             - "http*://*.cfapps.us10.hana.ondemand.com/**"

### New Resources Here ###

